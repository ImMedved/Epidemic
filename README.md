# Epidemic Engine v1.0

Current repository state matches `dev-log/plan.md` Step 1: the stable runtime base is organized under `EngineBase/` with separate module targets for `Foundation`, `Memory`, `Diagnostics`, `Core`, `Platform`, `Input`, `RHI`, and `RHI_D3D11`, plus smoke apps and grouped tests.